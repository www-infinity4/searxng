use_default_settings: true

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  port: 8080
  bind_address: "0.0.0.0"
  limiter: false
  image_proxy: false
  secret_key: "infinity-phi-cloudflare-container"
